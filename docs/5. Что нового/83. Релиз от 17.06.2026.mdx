---
slug: /changelog/latest
---

import ploskformImage from "@site/docs/resources/igorka/ploskform.png";
import jilkomnImage from "@site/docs/resources/igorka/jil_komn.png"; 
import gorodImage from "@site/docs/resources/igorka/gorod.png"; 
import raschetImage from "@site/docs/resources/igorka/raschet.png";
import gorkaImage from "@site/docs/resources/igorka/gorka.png";
import vaznoImage from "@site/docs/resources/igorka/vazno.png";
import scenaImage from "@site/docs/resources/igorka/scena.png";
import scenarezultImage from "@site/docs/resources/igorka/scenarezult.png";

***

## Обновления Formit ПД

### Инсоляционная горка

Для работы с инсоляционной горкой необходимо в Revit настроить квартирографию и отстроить окружающую застройку, включая: перекрытие, окна, стены.

:::info Важно
На инсоляционную горку влияют только жилые комнаты и квартиры с типом *Жилое*.
:::

**1. Построение формы**

С помощью семейства *”Форма”* выстроить границы будущей *“застройки”*, оставляя ее плоской (<ImageLink to="ploskform"/>).

<Image image={ploskformImage} id="ploskform" width="100%" title="Плоская форма"/>

**2. Экспорт модели**

Во вкладке Formit переходим в *“Экспортер”* и выполняем все шаги экспорта.

Более подробно об экспорте вы можете ознакомиться по [ссылке](https://docs.formit.pro/docs/manual/exporter/model-export).

**3. Открытие проекта**

Переходим в Сервис и открываем ранее экспортированный проект.

**4. Выбор помещений для расчета**

:::info Важно
Для того, чтобы Сервис мог выстроить инсоляционную горку, необходимо произвести расчет инсоляции окружающей застройки.
:::

Для быстроты расчета рекомендуем в *“Дереве объектов”* через поиск отфильтровать все жилые помещения (спальни, гостиные, детские комнаты), которые необходимо учитывать по СанПиН 1.2.3685-21 "Гигиенические нормативы и
требования к обеспечению безопасности и (или) безвредности для человека факторов среды обитания".

Также, рекомендуем считать первый и типовой этажи (<ImageLink to="jil-komn"/>).

<Image image={jilkomnImage} id="jil-komn" width="60%" title="Поиск жилых помещений"/>

**5. Настройка и расчет инсоляции**

Переходим во вкладку *“Настройки инсоляции”*. Нажимаем *“Указать место”*, в строке поиска вводим нужный город для расчета. Все данные будут подтянуты автоматически
(<ImageLink to="gorod"/>).

<Image image={gorodImage} id="gorod" width="30%" title="Выбор местоположения"/>

Нажимаем кнопку *“Рассчитать инсоляцию”* (<ImageLink to="raschet"/>).

<Image image={raschetImage} id="raschet" width="30%" title='Кнопка "Расчитать инсоляцию"'/>

**6. Настройка инсоляционной горки**

После завершения расчета возвращаемся в *“Настройки инсоляции”*.

Прокрутив вниз бегунок вы увидите *“Настройки инсоляционной горки”* (<ImageLink to="gorka"/>). Здесь вы можете настроить размер ячейки/столбца и максимальную высоту будущей инсоляционной горки.

<Image image={gorkaImage} id="gorka" width="30%" title="Настройки инсоляционной горки"/>

:::info Важно
Если не выполнен предыдущий шаг, то изменение настроек и кнопка *“Рассчитать горку”* будут недоступны (<ImageLink to="vazno"/>).

<Image image={vaznoImage} id="vazno" width="40%" title="Настройки инсоляционной горки"/>
:::

**7. Расчет инсоляционной горки**

Нажимаем *“Рассчитать горку”*. В сцене формируется объём, рассчитанный с учётом влияния окружающей застройки (<ImageLink to="scena"/>).

<Image image={scenaImage} id="scena" width="100%" title="Инсоляционная горка"/>

Вы можете оценить влияние на окружение, нажав на окно. Жёлтым цветом отображаются интервалы, которые были зарезаны формой так, 
чтобы норматив для помещения/квартиры окружающей застройки соблюдался (<ImageLink to="scenarezult"/>).

<Image image={scenarezultImage} id="scenarezult" width="100%" title="Результаты"/>

:::info Текущие ограничения
Сейчас построение выполняется без автоматического перебора альтернативных вариантов для помещений, где интервал инсоляции влияет на итоговую форму.

В следующих версиях будет доступно редактирование интервалов инсоляции — пользователь сможет вручную снижать их до минимальных нормативно требуемых и точнее настраивать форму расчётного объёма.
:::

